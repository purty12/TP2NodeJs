extends layout

block content
  h1 #{title}: #{bookinstance._id}

    div(style='margin-left:20px;margin-top:20px')

      h4 Id

    form(method='POST' action='')
      div.form-group
        input#bookinstanceid.form-control(name='bookinstanceid', required='true', value=bookinstance._id )

      button.btn.btn-primary(type='submit') Delete
